# Refuse to install under a Node major we do not support. The Electron main
# process loads native modules (better-sqlite3) that are ABI-specific; a
# mismatched system Node is the classic "works on my machine only" failure.
engine-strict=true
