{
  "name": "@shipyard/connectors",
  "version": "0.1.0",
  "private": true,
  "description": "Connecting a project to somebody else's service, and knowing when to ask",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "test": "tsx harness/test-connectors.ts"
  },
  "dependencies": {
    "@shipyard/shared": "*"
  },
  "devDependencies": {
    "@shipyard/capability-resolver": "*",
    "@shipyard/verification-runner": "*"
  }
}
