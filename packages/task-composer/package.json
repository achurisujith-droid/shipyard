{
  "name": "@shipyard/task-composer",
  "version": "0.1.0",
  "private": true,
  "description": "Turns plans, gaps and failing checks into work the agent can do",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "test": "tsx harness/test-tasks.ts"
  },
  "dependencies": {
    "@shipyard/shared": "*"
  }
}
