export default function Home() {
  return (
    <main>
      <h1>Your app</h1>
      <p>
        This is the starting point. Nothing here is precious — it is meant to be
        replaced by whatever you are building.
      </p>
      <p>
        Components you install from the Shipyard library add their own pages,
        routes and database tables. They come with tests that run after they are
        installed, so you can tell whether they still work rather than assuming.
      </p>
    </main>
  );
}
